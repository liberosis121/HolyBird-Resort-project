# Holybird-resort-project
HolyBird Resort Management System — a desktop application developed with C# and WinForms (.NET Framework). It manages reservations, customers, rooms, and transactions efficiently. The system uses SQL Server as the backend database and applies ADO.NET for data connectivity and transaction handling.
